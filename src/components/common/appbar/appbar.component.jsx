import React from 'react';
import './appbar.styles.scss';

const AppBar = ({ title, subPage }) => {
  return (
    <div className="app-bar-wrapper">
      <div className="app-bar"></div>
    </div>
  );
};

export default AppBar;
